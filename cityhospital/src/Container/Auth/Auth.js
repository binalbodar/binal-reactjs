import React from 'react';
import { Button } from 'reactstrap';

function Auth(props) {
    return (
        <div>
            <Button color="danger">Danger!</Button>
        </div>
    );
}

export default Auth;